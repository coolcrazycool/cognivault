# План исправлений по фидбеку пользователей (2026-08-26)

> Источник: `Вопросы_для_агента.xlsx` — 60 вопросов после двух недель тестирования + два
> скриншота с комментариями. Разбор каждого ответа — в `Вопросы_для_агента_с_анализом.xlsx`,
> колонки F/G/H. Диагностика механизмов — аудит кода + офлайн-замеры на снапшоте корпуса
> (127 страниц, 920 чанков, 260 секций).
>
> Предыдущий план — `RAG_QUALITY_PLAN.md` (2026-07-31). Он не отменяется, но приоритеты
> ниже построены заново: измерения показали, что главное узкое место лежит не там, где
> предполагалось.

---

## 0. Главный вывод: качество теряется ПОСЛЕ поиска

Измеренный ретривал: **hit@1 = 0.82, hit@5 = 0.97, hit@10 = 0.99**. Нужный фрагмент почти
всегда находится. Он гибнет дальше по конвейеру:

| Стадия | Что происходит | Потеря |
|---|---|---|
| `dedupeSections` | оставляет **один** чанк на секцию | 660 из 920 чанков (71.7%) |
| окно `section_max_chars = 4000` | 44 секции из 260 длиннее окна | эти секции держат **70.6%** текста базы |
| грейдер | порог 4 вырождается в 3; `keep_top=2` не фильтруется | 40% контекста — сырая выдача |
| `_MAX_CONTEXT_BLOCKS = 5` | потолок любого ответа | 5 источников максимум |

**Практический смысл:** улучшать ранжирование сейчас бессмысленно — ответ упирается в
арифметику окна, а не в качество поиска.

Самый наглядный замер — вопрос №31 («для каких моделей финэффект НЕ рассчитывается»).
Все 7 моделей лежат в **одной** секции (24 379 символов), в 7 чанках, разнесённых на
16 762 символа. В окно 4000 помещается **максимум 3 из 7**. Агент назвал ровно 3.

---

## Статус (сверено по коду 2026-08-31)

**Сделано:**

- ✅ **Волна 0** — параметры в обоих ConfigMap и в дефолтах кода.
- ✅ **1.1 баг frontmatter** — починен; `labels` → `tags`; маскировавшие его тесты переписаны.
- ✅ **1.2 архив** — поле `archived` в payload + индекс + `include_archived` (выключено по умолчанию).
- ✅ **1.3 потолок контекста** — `n_hits` считается на кандидатах поиска, а не на переживших отбор.
- ✅ **1.4 грейдер** — пропущенный id ≠ упавший батч; фолбэк-порог логируется; страховка
  стала условной (`keep_top=1` и только при оценке ≥ 3).
- ✅ **Волна 2 (частично)** — `answer_shape` в condense **плюс** детерминированный
  `detect_shape()`, который работает и на первом ходе, где condense пропускается.
- ✅ **Волна 3** — промпт: полнота, аббревиатуры, предпосылки, форма ответа; убраны
  внутренние противоречия (отказ-вступление, безусловная оговорка о неполноте).
- ✅ **Волна 4 (частично)** — `ac:caption`, `ri:user`, alt-текст картинок.
- ✅ **Метрика полноты** — `item_recall`, детерминированная, без судьи.
- ✅ **Грейдер судит то, что уедет в контекст** — превью строится из чанка + `section_text`.

**Осталось. Список закрыт: разделять маршруты решено НЕ делать** — вместо отдельного
пути под перечисления улучшаем общий. Пункт «list-маршрут через `/vault/content`»
снят с плана.

| # | Что | Почему важно | Стоимость |
|---|-----|--------------|-----------|
| 1 | `dedupeSections` не должен схлопывать `table_rows` (`service.ts:450-464`) | Секция таблицы представлена одним чанком, остальные строки не доезжают. Ядро проблемы перечислений — и чинится в общем пути | малая |
| 2 | Глубина `fetchLimit` при `group_by_section` (`service.ts:86-92`) | 80 слотов затапливаются одной страницей: у реестра 103 чанка, после дедупа до грейдера доезжает горстка вместо 40. Источник «каждый раз разный набор» | средняя |
| 3 | Стемминг в `WORD_PATTERN` (`service.ts:116`) | Защита от схлопывания клонов не срабатывает: «вечные» ≠ «вечный» | малая |
| 4 | Аббревиатуры в `bm25.ts` | `ФП ДО` → `фп`, `АСУ` → `ас`, `ЕФС-2` → `ефс`. Требует бампа `BM25_SCHEME_VERSION` + переиндексации | малая + переиндексация |
| 5 | Отдельный `parentId` на row-group (`chunker.ts:1027-1037`) | Альтернатива п.1, но меняет идентичность чанков → переиндексация | средняя + переиндексация |
| 6 | Глоссарий аббревиатур из корпуса | Лечит №12 и №59 фидбека и ~10 из 16 расхождений vocabulary gap | средняя |
| 7 | `dbsf` вместо `rrf` | Замерено: hit@1 0.8351 против 0.8247. Одна строка, но нужен прогон на клиентском сете | малая |
| 8 | Кроссэнкодер `bge-reranker-v2-m3` | +4.7 nDCG@10 по RusBEIR. Веса запекаются на GH-сборке | большая |
| 9 | Синхронизация вложений (`sync_attachments=False`) | 234 ссылки `![]()` ведут в никуда | средняя |
| 10 | Резолв ФИО через `/rest/api/user?key=` | В storage-XHTML имён нет вовсе | малая |
| 11 | **Первый прогон `tools/eval/run.py`** | `reports/` пуст. Без baseline всё выше — вера | нужен стенд |

⚠️ **Фильтр архива требует переиндексации.** У существующих точек поля `archived` нет,
а `match` не матчит отсутствующий ключ — исключение сделано через `must_not`, поэтому
старые точки продолжают находиться. До переиндексации архив подмешивается.

---

## Волна 0 — правки без деплоя (сегодня, обратимо)

Всё меняется в пользовательском конфиге (`settings.py:249-270`), эффект виден сразу.
Это не решение, а измерение: покажет, сколько именно даёт снятие потолка.

| Ключ | Сейчас | Ставим | Зачем |
|---|---|---|---|
| `rag.section_max_chars` | 4000 | **12000** | №31 должен дать 7/7 вместо 3/7 |
| `rag.max_context_chars` | 24000 | **48000** | иначе расширенные секции не влезут |
| `rag.grader_keep_top` | 2 | **0** | убрать безусловный добор нефильтрованного |
| `rag.grader_threshold` | 4 | 4 | оставить, но см. Волну 1 (фолбэк на 3) |
| `prompts.system` | — | +4 правила | см. Волну 3 |

**Проверить на:** №31, №51, №52, №30 и скриншоте 2. Если №31 даёт 7/7 — причина №1
подтверждена, и Волна 1 обязательна. Если нет — вернуться к диагностике.

⚠️ Цена: контекст на ход вырастет примерно вдвое → задержка и расход токенов. Это
временный замер, а не целевая конфигурация.

---

## Волна 1 — баги и потолки (высокий эффект, малый объём)

### 1.1 Frontmatter не доезжает до индекса — подтверждённый баг

`src/lib/vault.ts:279-281` уже срезает frontmatter и возвращает голое тело.
`src/plugins/pipeline.ts:459` парсит frontmatter **второй раз** — на срезанном теле.
Воспроизведено запуском: во втором проходе ключей **ноль всегда**.

Следствие: `tags/project/status/type` пусты у **каждого** документа, `extra_metadata = "{}"`,
все четыре фильтра поиска (`service.ts:729-745`) не могут сматчиться никогда, keyword-индексы
на них (`qdrant.ts:137-144`) — расход памяти впустую.

- Читать frontmatter один раз, пробрасывать распарсенные данные в пайплайн.
- Сопоставить ключи: конвертер пишет `title, source, confluence_id, space, source_url,
  version, last_updated, ancestors, labels, content_hash` (`convert.py:296-312`), пайплайн
  читает `tags/project/status/type`. **Пересечение — пустое.** Завести явный маппинг.
- Развернуть в индексируемые поля: `archived` (bool, из `ancestors`), `last_updated`,
  `source_url`, `labels`.
- **Починить тесты:** `pipeline.test.ts:703,816,920` мокают `readContent` контентом
  *вместе* с frontmatter — поэтому баг и жил.

**Чинит:** №53, №54, №55, №60 и делает возможным всё остальное про архив.

### 1.2 Архив перестаёт конкурировать с актуальным

В корпусе 11 архивных страниц из 127 — **52 чанка из 920 (5.7%)**. `collapseCrossFileDuplicates`
их не трогает (0 пар с Jaccard ≥ 0.8 к живым), то есть слоты они выигрывают честно.

- По умолчанию исключать `Архив/` из выдачи; включать только при явном запросе истории.
- Путь в вольте уже содержит «Архив» (`convert.py:236-249`), фильтр `filters.folder` по
  префиксу в API уже есть (`service.ts:420-428`) — **работает независимо от 1.1**, можно
  начать с него.
- UI сейчас фильтры вообще не передаёт (`cognivault.py:117-156`) — добавить.
- Сортировать финальный контекст по дате, новые первыми.

**Чинит:** №54, №55, №60.

### 1.3 Снять потолок контекста

- `dedupeSections` (`service.ts:450-464`) — не применять к `content_kind == table_rows`;
  либо давать row-group собственный `parentId` (`chunker.ts:1027-1037`).
- `_MAX_CONTEXT_BLOCKS = 5` (`rag.py:213`) и `_MAX_SELECTED = 5` (`rag_pipeline.py:93`) —
  вынести в конфиг, поднять для перечислений.
- `_decide_file_depth` (`rag.py:441-444`): `n_hits` считается **после** дедупа, который
  только что оставил один хит, поэтому условие `n_hits >= 3` не срабатывает никогда, а
  страница длиннее 6000 символов не разворачивается целиком. Считать `n_hits` до дедупа.

**Чинит:** №30, №31, №51, №52, скриншот 1.

### 1.4 Три дыры в грейдере

- `rag_pipeline.py:674` — `keep = above(4) or above(3)`: если четвёрок нет, порог молча
  вырождается в 3, а 3 по его же шкале — «по теме, но скорее **не** нужен». Убрать
  автоматический фолбэк либо сделать его явным и логируемым.
- `rag_pipeline.py:679-694` — `grader_keep_top=2` отдаёт 2 из 5 слотов сырой выдаче
  безусловно, даже при оценке 1. Отключить (Волна 0) или ограничить оценкой ≥ 3.
- `rag_pipeline.py:653-657` — фрагмент, не упомянутый в JSON грейдера, оставляется
  **безусловно**: ленивый ответ модели превращает фильтр в no-op. Инвертировать: не
  упомянут → отброшен.
- `rag_pipeline.py:341-345` — превью схлопывает переносы, markdown-таблица становится
  лентой пайпов, и из 600 символов видно шапку и обрывок первой строки. Реестровые чанки
  систематически недооцениваются. Для `table_rows` сохранять построчную структуру.

**Чинит:** скриншот 2, №33, №53, №56.

### 1.5 Стемминг в защите от схлопывания клонов

`WORD_PATTERN` (`service.ts:115`) не стеммирует, поэтому оговорка «копия несёт слово
запроса, которого нет у выжившего» не срабатывает: «вечные» из вопроса не равно «вечный»
в документе. Из 28 страниц с «Поток вечный» **10 пар** имеют Jaccard ≥ 0.8 и схлопываются
вслепую. Отсюда «каждый раз разный неполный набор».

Применять тот же стеммер, что в `bm25.ts`.

**Чинит:** скриншот 1 (нестабильность).

---

## Волна 2 — отдельный маршрут для «перечисли всё»

Сейчас такого пути нет: `top-k = 5` независимо от размера реестра. `corpus_scope.match_meta`
(`corpus_scope.py:164-374`) работает через `fullmatch` по ~35 regex, поэтому «перечисли всё
из реестра X» ему недоступно **по построению**.

1. Классификатор класса запроса (факт / перечисление / процедура / сравнение). TF-IDF + SVM
   достаточно, LLM и GPU не нужны.
2. Для перечислений — не top-k, а выдача секции или документа **целиком**: endpoint
   `GET /api/vault/content` уже существует (`vault/routes.ts:149-161`), cap по размеру нет.
3. Для процедур («как заполнять», «расскажи принцип») — тоже целая секция: в №51 описание
   zip лежит на офсетах 11 004–12 393 секции длиной 12 437, то есть в самом хвосте, куда
   центрированное окно не дотягивается.

⚠️ **Скриншот 1 требует решения и на стороне базы знаний.** Замер: «Поток вечный»
встречается в **28 разных файлах по одному разу** — это ячейка в описании каждой витрины.
Полный ответ требует 28 документов. Просьба «дай ссылку на реестр вечных потоков» в текущем
корпусе может быть невыполнима буквально: такого объекта нет. Либо заводим страницу-реестр,
либо агент честно отвечает «сводного реестра нет, вот список страниц».
**Это решение за владельцем базы, не за кодом.**

---

## Волна 3 — промпт генерации (дёшево, без деплоя)

`SYSTEM_PROMPT` (`rag.py:47-62`) редактируется пользователем. В нём **нет ничего** про
четыре класса провалов:

1. **Проверка предпосылок.** Вопрос «почему X» → сначала подтвердить X по источникам.
   №49: обе секции с фактом «10 минут» короче окна (3175 и 2034 символа) — они доехали бы
   целиком, но модели никто не велел проверять посылку, и она построила каузальное
   объяснение поверх непроверенного утверждения. Включать **по интенту**, не как
   обязательный проход: методы под ложные предпосылки дают >90% на них ценой падения
   ниже 50% на нормальных вопросах.
2. **Запрет расшифровывать аббревиатуры без источника.** №59: «ФП ДО» в корпусе есть
   (`Сервис получения ФИДов.md:20,22`), но **нигде не расшифровано** — «Фабрика Продуктивного
   Деплоя» взята из памяти модели. №1 («Batch Model Processing Framework») — тот же механизм,
   там просто угадала. Существующая формулировка «не используй собственные знания» моделью
   как запрет на расшифровку не читается.
3. **Полнота и честная неполнота.** Требование перечислить всё, что есть в источниках, +
   обязательная оговорка «список может быть неполон», когда источник — фрагмент таблицы.
   №31 читается как исчерпывающий список, хотя это 3 из 7.
4. **Запрет ложного дисклеймера.** №56: правильный ответ **был найден**, но обрамлён
   «нет прямого ответа» сверху и лишним хи-квадратом снизу. Ср. №5, №22, №50 — паттерн
   устойчивый.

Плюс:
- **Единая терминология в ответе** (№52: «единая разметка» и «SQL-разметка» — одна сущность
  под двумя именами).
- **Запрет выдавать отсутствие за отрицание** (№58: «у metrix нет собственных параметров» —
  на деле «не нашёл»).
- **Не смешивать пользовательский и технический уровень** (№53).
- **Валидация маркеров цитат на выходе:** №34 содержит `[Источник базы знаний]` — маркер вне
  нумерации, некликабельный. №37 — придуманный процесс со ссылкой `[Источник 4]`.
- `finish_reason="length"` виден только в логе (`chat_routes.py:540-543`), UI на него не
  реагирует: ответ, обрезанный посреди перечисления, выглядит законченным. Показывать.

---

## Волна 4 — корпус и парсинг

- **Подписи к картинкам уничтожаются:** `convert.py:749-769` — `replace_with` сносит
  `<ac:caption>` вместе с `<ac:image>`. В дампе **198 картинок → ноль индексируемого текста**.
- **Вложения не синхронизируются** (`sync_attachments=False`, `sync.py:657`): все 234 ссылки
  `![]()` указывают на несуществующие файлы.
- **Alt-текст выпадает и на индексации:** `nodeToText` (`chunker.ts:274-299`) для mdast-узла
  `image` возвращает пустую строку.
- **Диаграммы** → `*[Диаграмма: имя]*` (`convert.py:622-630`); имена потоков внутри схемы
  теряются. Это ровно жалоба из №56.
- **Упоминания людей:** `<ri:user>` уходит в `else` (`convert.py:778-813`) → пустая строка.
  Осложнение: в storage-XHTML ФИО нет вообще, только `userkey` — нужен резолв через
  `/rest/api/user?key=`. **№57 окончательно не диагностирован**: возможен и второй механизм
  (`_DROP_MACROS` / `multiexcerpt-include`, `convert.py:694`, молча опустошает страницу).
  Нужна исходная страница.
- **Страницы-заглушки `[пусто]`** (№41) занимают слот в top-k — исключать из индекса.
- **BM25 теряет аббревиатуры** (`bm25.ts:130-145`): `tokenize('ФП ДО') → ['фп']`,
  `АСУ → 'ас'`, `ЕФС-2 → ['ефс']` (неотличимо от ЕФС-3). Правки маленькие, но требуют бампа
  `BM25_SCHEME_VERSION` и переиндексации.

---

## Волна 5 — то, что даёт прирост, но дороже

- **Кроссэнкодер-реранкер** `BAAI/bge-reranker-v2-m3` (Apache-2.0, MIRACL RU 67.61 — лучший
  из license-clean). RusBEIR: +4.7 nDCG@10 поверх BGE-M3. CPU ~0.3–0.7 с на 50 кандидатов,
  int8 ×1.9–2.2. Веса запекаются на GH-сборке. Реранкить top-50, не top-200. Схлопывание
  дублей делать **до** реранка.
  ⛔ jina-reranker-v2/v3 и jina-colbert-v2 — **CC-BY-NC, нельзя**.
- **Грейдер listwise вместо pointwise** — 0 дополнительных вызовов. Pointwise-кроссэнкодеры
  при большом K хуже first-stage в 44–53% случаев.
- **Глоссарий аббревиатур**, намайненный из корпуса (пары «Полное Название (АББР)»).
  Лечит №12, №59 и ~10 из 16 расхождений vocabulary gap.
- **Фьюжн `dbsf` вместо `rrf`** — одна строка, 73 секунды на прогон. В свипе даёт hit@1
  **0.8351** против прод **0.8247**. Не проверялось всерьёз.

---

## Чего НЕ делать

| Гипотеза | Почему нет |
|---|---|
| **Взвешенный RRF** `{rrf:{k,weights}}` | `weights` появились только в Qdrant **1.17.0**, на проме пин **1.16.3** — серверно недоступно. Вопрос закрыт, а не открыт. `k` доступен с 1.16.0, но замерен: `rrf-k60` hit@1 0.809 против прод 0.825 |
| **Отказ по порогу скора** | Невозможен: ROC AUC разделения отвечаемых и ловушек — dense 0.617, bm25 0.657, на сгенерированном сете 0.5065 (монетка). Отказ — работа грейдера |
| **Снимать breadcrumb / менять состав dense-текста** | Замерено: снятие breadcrumb — худший вариант (−0.032 hit@1); `prepend_title` даёт 0.739, но сигнал **инвертируется** на клиентском сете |
| **Выключать `group_by_section`** | Окупается: containment 0.758 → 0.930. Проблема не в группировке, а в кэпе 4000 поверх неё |
| **Откатывать центрирование окна** | Выиграло: +0.070 общий, **+0.186** на переразмерных секциях, 0 промахов якоря из 190 |
| **Трогать `INDEX_DOC_SUMMARY`** | Офлайн не решено: осмысленная аннотация (0.702) проигрывает length-matched болванке (0.713). Не менять без живого A/B |
| **Ждать прироста ранга от `collapseCrossFileDuplicates`** | По ранжированию ничья: 4 вопроса из 188, MRR +0.0005. Его польза в другом (дубли в top-5 0.42 → 0.01) |

Также неприменимо в контуре: LlamaParse, облачные реранкеры и судьи, ColBERT-стадия,
HHEM-2.1-open / MiniCheck / AlignScore / Lynx, Self-RAG, Astute RAG, `gigaragas` как
зависимость (проект мёртв).

---

## Что измерить

Всё, кроме п. 3, гоняется офлайн на `.rag-audit/latest/chunks.jsonl` без сети и кредов.

1. **Свип `section_max_chars`** на новой метрике *item-recall* для перечислений.
   `audit_window.py --caps` уже свипает 2000/4000/8000/16000/∞ × {centred, prefix}, но
   текущая метрика (containment термов) неполноту списка **не видит**. Нужен сет из ~30
   вопросов «перечисли все N» с эталоном-списком. Ожидание: №31 → 3/7 при 4000, 7/7 при 16000.
   **Это самый прямой способ подтвердить или опровергнуть причину №1.**
2. **Абляция `dedupeSections`:** как есть / выключен для `table_rows` / отдельный `parentId`
   на row-group. Ожидание: hit@1 факта не двигается, item-recall двигается резко.
3. **Первый в истории прогон `tools/eval/run.py` на живом стенде.** `tools/eval/reports/`
   **пуст — харнесс не запускался ни разу**, генерационного baseline не существует.
   39 вопросов × ~4 судейских вызова ≈ 160 вызовов, concurrency 2–3.
   Без этого любая правка промпта — вера.
4. **Словарь синонимов против vocabulary gap.** `audit_vocab.py --hooks` подключает
   трансформ запроса файлом. 16 из 35 пар расходятся при k=40; ~10 лечатся картой,
   остальные (paraphrase 4/5, colloquial 2/6) — граница метода.
5. **A/B реранкера** — `rerank(impl)` уже разведён в post-стадии. Планировать на +4.7
   nDCG@10, а не на цифры из вендорских блогов.
6. **Контаминация архива:** насколько часто архивный чанк входит в top-5 и доезжает до
   контекста. После починки 1.1/1.2 — повторить и померить цену по recall.
7. **`dbsf` против `rrf`** на клиентском сете, с вниманием к hit@3.

**Оговорка к цифрам:** плотную сторону офлайн-аудита меряет `multilingual-e5-base`, а не
EmbeddingsGigaR. Абсолютные hit@1/MRR в прод **не переносятся** — переносятся сравнения при
прочих равных. Закрывает ли гибрид vocabulary gap на боевом эмбеддере — **не измерено, и это
самое важное открытое число.** Снапшот корпуса от 2026-08-01 может расходиться с тем, что
тестировали пользователи.

---

## Порядок работ

| # | Что | Эффект | Объём |
|---|---|---|---|
| 1 | Волна 0 — конфиг + промпт | проверка гипотезы | часы, без деплоя |
| 2 | 1.1 баг frontmatter + тесты | разблокирует всё про архив | малый |
| 3 | 1.2 фильтр архива | №54, №55, №60 | малый |
| 4 | 1.4 грейдер | скриншот 2, №33, №53, №56 | малый |
| 5 | 1.3 потолок контекста | №30, №31, №51, №52 | средний |
| 6 | Волна 3 — промпт целиком | №37, №49, №52, №56, №58, №59 | малый |
| 7 | 1.5 стемминг | скриншот 1 (стабильность) | малый |
| 8 | Волна 2 — list-маршрут | скриншот 1, №30, №31 | большой |
| 9 | Волна 4 — парсинг | №41, №56, №57 | средний |
| 10 | Волна 5 — реранкер, глоссарий, dbsf | общий прирост | большой |

Пункты 2–4 и 6–7 независимы и делаются параллельно.
