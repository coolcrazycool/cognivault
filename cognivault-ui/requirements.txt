fastapi==0.121.3
uvicorn==0.34.0
httpx==0.28.1
python-multipart==0.0.20
# Pin starlette to a patched release (>= 0.49.1 fixes CVE-2025-62727 / CVE-2025-54121).
# fastapi 0.121.x allows starlette <0.51, so 0.49.3 resolves cleanly.
starlette==0.49.3
# Confluence source: storage-XHTML → Markdown conversion (later phases) + YAML
# front-matter. Pure-python, mature pins (SberOSC quarantine: >3 days old).
beautifulsoup4==4.12.3
markdownify==0.13.1
PyYAML==6.0.2
