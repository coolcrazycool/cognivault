---
phase: 09-toon-api-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/swagger.ts
  - src/plugins/__tests__/swagger.test.ts
  - src/app.ts
  - package.json
autonomous: true
requirements: [INF-02]

must_haves:
  truths:
    - "GET /docs returns 200 with Swagger UI HTML page"
    - "GET /docs/json returns valid OpenAPI 3.0 JSON spec"
    - "OpenAPI spec includes all registered route schemas"
    - "/docs endpoints are accessible without authentication"
    - "OpenAPI spec documents bearer auth security scheme"
  artifacts:
    - path: "src/plugins/swagger.ts"
      provides: "Swagger + Swagger UI plugin registration"
      exports: ["default"]
    - path: "src/plugins/__tests__/swagger.test.ts"
      provides: "Smoke tests for OpenAPI spec and Swagger UI"
      min_lines: 40
  key_links:
    - from: "src/plugins/swagger.ts"
      to: "@fastify/swagger"
      via: "import swagger"
      pattern: "import.*@fastify/swagger"
    - from: "src/app.ts"
      to: "src/plugins/swagger.ts"
      via: "app.register(swaggerPlugin)"
      pattern: "register.*swagger"
---

<objective>
Add auto-generated OpenAPI documentation with interactive Swagger UI to CogniVault.

Purpose: Agents and developers can discover API endpoints, schemas, and authentication requirements via /docs.
Output: Swagger plugin, Swagger UI at /docs, OpenAPI spec at /docs/json and /docs/yaml.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-toon-api-polish/09-CONTEXT.md
@.planning/phases/09-toon-api-polish/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app.ts:
```typescript
export async function buildApp(opts?: BuildAppOptions): Promise<FastifyInstance>
// Plugin order: error-handler -> auth -> [infrastructure] -> feature routes
// Swagger must be registered BEFORE feature routes to capture their schemas
```

From src/plugins/auth.ts:
```typescript
// skipAuth pattern — swagger-ui needs uiHooks to bypass auth
if ((request.routeOptions.config as unknown as Record<string, unknown>)?.skipAuth) { return; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install swagger packages and create swagger plugin with tests</name>
  <files>package.json, pnpm-lock.yaml, src/plugins/swagger.ts, src/plugins/__tests__/swagger.test.ts</files>
  <behavior>
    - Test: GET /docs returns 200 with HTML content (Swagger UI)
    - Test: GET /docs/json returns 200 with valid OpenAPI JSON (has openapi, info, paths keys)
    - Test: GET /docs without auth header returns 200 (no authentication required)
    - Test: OpenAPI spec includes bearerAuth security scheme
    - Test: OpenAPI spec includes at least one path from feature routes (e.g., /health or /api/vault)
  </behavior>
  <action>
    1. Run `pnpm add @fastify/swagger @fastify/swagger-ui`.
    2. Create `src/plugins/swagger.ts` as a Fastify plugin wrapped with `fp()` (name: 'swagger'):
       - Import `swagger` from `@fastify/swagger` and `swaggerUi` from `@fastify/swagger-ui`.
       - Register `swagger` with OpenAPI 3.0 config:
         - `openapi: '3.0.0'`
         - `info: { title: 'CogniVault API', description: 'Knowledge access layer for AI agents. Supports application/json and text/toon content types.', version: '1.0.0' }`
         - `components.securitySchemes.bearerAuth: { type: 'http', scheme: 'bearer' }`
         - `security: [{ bearerAuth: [] }]`
       - Register `swaggerUi` with:
         - `routePrefix: '/docs'`
         - `uiHooks: { onRequest: (_req, _reply, next) => next() }` — bypasses auth plugin
    3. Create `src/plugins/__tests__/swagger.test.ts`:
       - Build full app via `buildApp({ logger: false })` in beforeAll.
       - Test all behaviors listed above.
       - For the "includes paths" test, parse /docs/json response and check `Object.keys(result.paths).length > 0`.
    4. Update `src/app.ts`:
       - Import swaggerPlugin: `import swaggerPlugin from './plugins/swagger.js'`
       - Register AFTER auth but BEFORE feature routes: `await app.register(swaggerPlugin)` — placed after auth (and after toon if plan 01 already ran) but before vaultPlugin.
    5. Run full test suite.
  </action>
  <verify>
    <automated>pnpm test -- --run src/plugins/__tests__/swagger.test.ts</automated>
  </verify>
  <done>Swagger UI accessible at /docs without auth. /docs/json returns valid OpenAPI 3.0 spec with all route schemas. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `pnpm test` — full suite passes
- `pnpm check` — typecheck + lint + format pass
- GET /docs returns Swagger UI HTML
- GET /docs/json includes paths for health, vault, search, context routes
- GET /docs without Authorization header returns 200
</verification>

<success_criteria>
- OpenAPI spec auto-generated from TypeBox route schemas
- Swagger UI served at /docs, spec at /docs/json and /docs/yaml
- Docs endpoints skip authentication
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/09-toon-api-polish/09-02-SUMMARY.md`
</output>
