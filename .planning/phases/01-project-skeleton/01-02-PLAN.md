---
phase: 01-project-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/features/health/routes.ts
  - src/features/health/schemas.ts
  - src/features/health/__tests__/routes.test.ts
  - src/plugins/auth.ts
  - src/plugins/__tests__/auth.test.ts
  - src/app.ts
autonomous: true
requirements: [INF-01, API-04]

must_haves:
  truths:
    - "GET /health returns 200 with status, timestamp, uptime without auth"
    - "GET /ready returns 200 with status and timestamp without auth"
    - "Request to protected route without Authorization header returns 401"
    - "Request to protected route with invalid Bearer token returns 401"
    - "Request to protected route with valid Bearer token succeeds"
    - "Error response for 401 follows format { error: { code: UNAUTHORIZED, message: ... } }"
  artifacts:
    - path: "src/features/health/routes.ts"
      provides: "Health and readiness route handlers"
      exports: ["healthRoutes"]
      min_lines: 20
    - path: "src/features/health/schemas.ts"
      provides: "TypeBox schemas for health responses"
      exports: ["HealthResponseSchema", "ReadyResponseSchema", "healthSchema", "readySchema"]
    - path: "src/plugins/auth.ts"
      provides: "Bearer auth plugin with skipAuth support"
      contains: "verifyBearerAuth"
    - path: "src/features/health/__tests__/routes.test.ts"
      provides: "Tests for health endpoints"
      contains: "inject"
    - path: "src/plugins/__tests__/auth.test.ts"
      provides: "Tests for auth plugin"
      contains: "401"
  key_links:
    - from: "src/app.ts"
      to: "src/plugins/auth.ts"
      via: "registers auth plugin"
      pattern: "register.*auth"
    - from: "src/app.ts"
      to: "src/features/health/routes.ts"
      via: "registers health routes"
      pattern: "register.*health"
    - from: "src/features/health/routes.ts"
      to: "src/features/health/schemas.ts"
      via: "imports schemas for route definitions"
      pattern: "import.*schemas\\.js"
    - from: "src/plugins/auth.ts"
      to: "src/config.ts"
      via: "reads COGNIVAULT_API_KEY from config"
      pattern: "config\\.COGNIVAULT_API_KEY"
---

<objective>
Add health/readiness endpoints and API key authentication to the Fastify app.

Purpose: Deliver the two core Phase 1 capabilities -- unauthenticated health probes for Docker/K8s and Bearer token authentication for all other routes. This makes the service ready for Docker deployment in Plan 03.
Output: Working health/readiness routes, auth middleware, and comprehensive tests for both.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton/01-CONTEXT.md
@.planning/phases/01-project-skeleton/01-RESEARCH.md
@.planning/phases/01-project-skeleton/01-01-SUMMARY.md
@CLAUDE.md
@.claude/skills/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Health/readiness endpoints with schemas and tests</name>
  <files>src/features/health/schemas.ts, src/features/health/routes.ts, src/features/health/__tests__/routes.test.ts, src/app.ts</files>
  <behavior>
    - GET /health returns 200 with { status: "ok", timestamp: ISO string, uptime: number > 0 }
    - GET /health requires no Authorization header
    - GET /ready returns 200 with { status: "ready", timestamp: ISO string }
    - GET /ready requires no Authorization header
  </behavior>
  <action>
1. Create src/features/health/schemas.ts per RESEARCH.md code example: HealthResponseSchema (status literal "ok", timestamp string, uptime number), ReadyResponseSchema (status union "ready"|"not_ready", timestamp string). Export healthSchema and readySchema objects for route schema option.

2. Create src/features/health/routes.ts per RESEARCH.md code example: Named export `healthRoutes` function (async, takes FastifyInstance). Register GET /health with `config: { skipAuth: true }` and healthSchema. Register GET /ready with `config: { skipAuth: true }` and readySchema. Phase 1 readiness always returns "ready" (no external deps to check yet).

3. Update src/app.ts: Register health routes via `await app.register(import('./features/health/routes.js'))`. Place after error handler plugin registration.

4. Create src/features/health/__tests__/routes.test.ts per RESEARCH.md test example: Set env vars in beforeAll (COGNIVAULT_API_KEY=test-key, VAULT_PATH=/tmp/test-vault). Build app with logger false. Test GET /health returns 200 with correct shape. Test GET /ready returns 200 with status "ready". Both tests should NOT send Authorization header to prove auth is skipped.

Follow TDD: write tests first, run them (should fail since routes don't exist yet), then implement, run again (should pass).
  </action>
  <verify>
    <automated>cd /Users/cytryx/ClaudeProject/cognivault && pnpm test -- --run src/features/health/__tests__/routes.test.ts</automated>
  </verify>
  <done>Health endpoint returns 200 with status/timestamp/uptime. Readiness endpoint returns 200 with status/timestamp. Both work without auth headers. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API key auth plugin with skipAuth and tests</name>
  <files>src/plugins/auth.ts, src/plugins/__tests__/auth.test.ts, src/app.ts</files>
  <behavior>
    - Request without Authorization header to protected route returns 401 with { error: { code: "UNAUTHORIZED", message: ... } }
    - Request with invalid Bearer token returns 401
    - Request with valid Bearer token (matching COGNIVAULT_API_KEY) returns success
    - Health/readiness endpoints skip auth entirely (already tested in Task 1)
  </behavior>
  <action>
1. Create src/plugins/auth.ts per RESEARCH.md Pattern 2: Wrap with fastify-plugin (fp) for global visibility. Register @fastify/bearer-auth with `keys: new Set([config.COGNIVAULT_API_KEY])`. Use `addHook: false` to prevent automatic hook. Add onRequest hook that checks `request.routeOptions.config?.skipAuth` -- if true, skip auth; otherwise call `(fastify as any).verifyBearerAuth(request, reply)`.

   IMPORTANT: If `addHook: false` is not supported by @fastify/bearer-auth at the installed version, fall back to a custom onRequest hook using `crypto.timingSafeEqual()` for key comparison (see RESEARCH.md open question #1). Check the plugin source/docs during implementation.

2. Update src/app.ts: Register auth plugin AFTER error handler but BEFORE feature routes. This ensures: errors formatted first, auth runs on all routes, routes registered last.

   Final plugin order in app.ts:
   - error-handler plugin
   - auth plugin
   - health routes (and future feature routes)

3. Create src/plugins/__tests__/auth.test.ts: Register a test-only protected route inside the test (e.g., GET /test-protected that returns { ok: true }). Tests:
   - No Authorization header -> 401 with error.code "UNAUTHORIZED"
   - Authorization: Bearer wrong-key -> 401
   - Authorization: Bearer test-key -> 200 with { ok: true }
   - Health endpoint still returns 200 without auth (regression check)

Follow TDD: write tests first, run (fail), implement auth plugin + update app.ts, run again (pass).
  </action>
  <verify>
    <automated>cd /Users/cytryx/ClaudeProject/cognivault && pnpm test -- --run src/plugins/__tests__/auth.test.ts</automated>
  </verify>
  <done>Auth plugin rejects requests without valid Bearer token (401). Auth plugin accepts requests with valid token. Health/readiness endpoints bypass auth. Error responses follow { error: { code, message } } format. All tests pass.</done>
</task>

</tasks>

<verification>
1. `pnpm test -- --run` -- all tests pass (health + auth)
2. `pnpm build` -- TypeScript compiles cleanly
3. `pnpm check` -- Biome lint + format + typecheck all pass
4. Manual smoke: start server, `curl localhost:3000/health` returns 200, `curl localhost:3000/ready` returns 200, `curl localhost:3000/test` returns 401
</verification>

<success_criteria>
- GET /health returns 200 with status, timestamp, uptime -- no auth required
- GET /ready returns 200 with ready status -- no auth required
- Unauthenticated requests to any non-health route return 401 with structured error
- Valid Bearer token grants access
- All tests pass, TypeScript compiles, Biome checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-02-SUMMARY.md`
</output>
