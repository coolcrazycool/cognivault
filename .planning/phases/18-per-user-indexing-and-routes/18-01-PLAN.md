---
phase: 18-per-user-indexing-and-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/metrics.ts
  - src/plugins/embedding.ts
  - src/plugins/qdrant.ts
  - src/plugins/db.ts
  - src/config.ts
  - src/plugins/__tests__/metrics.test.ts
  - src/plugins/__tests__/embedding.test.ts
autonomous: true
requirements: [OBS-01]

must_haves:
  truths:
    - "Every per-user metric carries a user_id label on increment"
    - "Global metrics (registry_users, registry_reloads, auth_failures) remain unlabeled"
    - "Each user gets their own EmbeddingProvider initialized with their openaiKey"
    - "Embedder is recreated when user's openaiKey changes (user-updated event)"
    - "On user-removed, all user-labeled metric values are cleaned up via .remove()"
    - "Qdrant plugin creates collection without depending on global embedder"
    - "Pipeline code can access user DB via fastify.getUserDbById(userId) outside request context"
  artifacts:
    - path: "src/plugins/metrics.ts"
      provides: "MetricsCollection with user_id labels, contextPacks counter, removeUserMetrics helper"
      exports: ["MetricsCollection (interface via module augmentation)"]
    - path: "src/plugins/embedding.ts"
      provides: "Per-user EmbeddingProvider Map with getUserEmbedder(userId) decorator"
      exports: ["embeddingPlugin (default)"]
    - path: "src/plugins/qdrant.ts"
      provides: "Collection creation using DIMENSION_MAP[config.EMBEDDING_MODEL] instead of fastify.embedder.dimensions"
    - path: "src/plugins/db.ts"
      provides: "getUserDbById(userId) fastify-level accessor for pipeline use outside request context"
    - path: "src/config.ts"
      provides: "OPENAI_API_KEY as optional (per-user keys now)"
  key_links:
    - from: "src/plugins/embedding.ts"
      to: "src/lib/user-registry.ts"
      via: "registry events (user-added, user-removed, user-updated)"
      pattern: "registry\\.on\\('user-(added|removed|updated)'"
    - from: "src/plugins/qdrant.ts"
      to: "src/lib/embedding.ts"
      via: "DIMENSION_MAP import for collection size"
      pattern: "DIMENSION_MAP\\[config\\.EMBEDDING_MODEL\\]"
    - from: "src/plugins/db.ts"
      to: "pipeline (downstream)"
      via: "getUserDbById fastify decoration"
      pattern: "fastify\\.decorate\\('getUserDbById'"
---

<objective>
Refactor metrics, embedding, qdrant, db, and config plugins for multi-tenant foundation.

Purpose: These plugins are dependencies for the pipeline and indexer rewrites in Plan 02, and for route migration in Plan 03. Metrics needs user_id labels (OBS-01). Embedding needs per-user Map. Qdrant needs to decouple from global embedder. DB needs a fastify-level accessor for pipeline use outside request context. Config needs OPENAI_API_KEY made optional since all embedding now uses per-user keys.

Output: Five refactored plugins ready for per-user indexing pipeline and route migration.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-per-user-indexing-and-routes/18-CONTEXT.md
@.planning/phases/18-per-user-indexing-and-routes/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/lib/embedding.ts:
```typescript
export const DIMENSION_MAP: Record<string, number> = {
  'text-embedding-3-small': 1536,
  'text-embedding-3-large': 3072,
};

export interface EmbeddingProvider {
  embed(texts: string[]): Promise<number[][]>;
  readonly dimensions: number;
}

export class OpenAIEmbeddingProvider implements EmbeddingProvider {
  constructor(opts: { apiKey: string; baseUrl?: string; model: string });
  async validate(): Promise<void>;
}
```

From src/lib/user-registry.ts:
```typescript
export type UserRecord = {
  userId: string;
  apiKey: string;
  vaultPath: string;
  openaiKey: string;
  obsidian: { email: string; password: string; vault: string; token?: string };
};

interface RegistryEvents {
  'user-added': [user: UserRecord];
  'user-removed': [user: UserRecord];
  'user-updated': [user: UserRecord, previous: UserRecord];
}
```

From src/plugins/db.ts:
```typescript
type DbInstance = BetterSQLite3Database<typeof schema>;

declare module 'fastify' {
  interface FastifyRequest {
    getUserDb: () => DbInstance;
    getUserQdrant: () => TenantQdrantClient;
  }
}
```

From src/plugins/metrics.ts (current -- to be refactored):
```typescript
interface MetricsCollection {
  searchDuration: Histogram<'type'>;
  searchRequests: Counter<'type'>;
  indexQueueDepth: Gauge;
  staleVectorCleanups: Counter;
  embeddingRequests: Counter;
  chunksProcessed: Counter;
  pipelineDuration: Histogram;
  promRegistry: Registry;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor metrics plugin with user_id labels and embedding plugin to per-user Map</name>
  <files>
    src/plugins/metrics.ts
    src/plugins/__tests__/metrics.test.ts
    src/plugins/embedding.ts
    src/plugins/__tests__/embedding.test.ts
  </files>
  <behavior>
    - Metrics: searchDuration.startTimer({ type: 'semantic', user_id: 'alice' }) records with both labels
    - Metrics: searchRequests.inc({ type: 'hybrid', user_id: 'bob' }) records with both labels
    - Metrics: indexQueueDepth.set({ user_id: 'alice' }, 5) sets per-user gauge
    - Metrics: staleVectorCleanups.inc({ user_id: 'alice' }) records with user_id
    - Metrics: embeddingRequests.inc({ user_id: 'alice' }) records with user_id
    - Metrics: chunksProcessed.inc({ user_id: 'alice' }, 3) records with user_id
    - Metrics: pipelineDuration.startTimer({ user_id: 'alice' }) records with user_id
    - Metrics: contextPacks counter exists and records with user_id label
    - Metrics: removeUserMetrics('alice') removes all label combinations for that userId
    - Metrics: /metrics endpoint still works and skips auth
    - Embedding: getUserEmbedder('alice') returns EmbeddingProvider for alice
    - Embedding: getUserEmbedder('unknown') throws Error
    - Embedding: user-added event creates new embedder
    - Embedding: user-removed event deletes embedder
    - Embedding: user-updated with changed openaiKey recreates embedder
    - Embedding: user-updated with same openaiKey does NOT recreate embedder
    - Embedding: onClose cleans up all embedders
  </behavior>
  <action>
    **Metrics plugin (src/plugins/metrics.ts):**
    1. Update MetricsCollection interface: all per-user metrics add 'user_id' to their generic type params:
       - `searchDuration: Histogram<'type' | 'user_id'>`
       - `searchRequests: Counter<'type' | 'user_id'>`
       - `indexQueueDepth: Gauge<'user_id'>`
       - `staleVectorCleanups: Counter<'user_id'>`
       - `embeddingRequests: Counter<'user_id'>`
       - `chunksProcessed: Counter<'user_id'>`
       - `pipelineDuration: Histogram<'user_id'>`
       - Add `contextPacks: Counter<'user_id'>` (NEW metric for context pack assembly tracking)
       - Add `removeUserMetrics: (userId: string) => void`
    2. Add 'user_id' to labelNames array on each metric constructor call
    3. Create new contextPacks counter: name 'cognivault_context_packs_total', help 'Total context packs assembled', labelNames ['user_id']
    4. Create removeUserMetrics function that calls .remove() with all label combinations for that userId:
       - searchDuration: remove for each type (semantic, hybrid, lexical)
       - searchRequests: remove for each type (semantic, hybrid, lexical)
       - indexQueueDepth, staleVectorCleanups, embeddingRequests, chunksProcessed, pipelineDuration, contextPacks: remove with just user_id
    5. Add contextPacks and removeUserMetrics to the fastify.decorate('metrics', ...) object
    6. Do NOT skip validate() -- tests mock the embedding provider so validate is irrelevant in tests

    **Embedding plugin (src/plugins/embedding.ts):**
    1. Replace global `fastify.embedder` with `fastify.getUserEmbedder(userId)` in FastifyInstance augmentation
    2. Remove global EmbeddingProvider creation
    3. Create `Map<string, EmbeddingProvider>` for per-user embedders
    4. Initialize embedders for all existing users from registry at plugin init (skip validate() -- see Research pitfall 5)
    5. Listen for registry events: user-added creates embedder, user-removed deletes embedder, user-updated recreates if openaiKey changed
    6. Decorate fastify with getUserEmbedder that looks up from Map, throws if not found
    7. Clean up Map on onClose hook
    8. Use config.OPENAI_BASE_URL and config.EMBEDDING_MODEL from config (same for all users -- only apiKey differs)
    9. fp() dependencies: ['registry'] (needs registry.getAllUsers() and event listeners)

    **Tests:**
    - metrics.test.ts: Update existing tests to pass user_id with every metric call. Add test for removeUserMetrics. Add test for contextPacks counter.
    - embedding.test.ts: New test file. Mock OpenAIEmbeddingProvider. Test per-user lifecycle: create at init, user-added, user-removed, user-updated, getUserEmbedder lookup, error on unknown user.
  </action>
  <verify>
    <automated>cd /Users/cytryx/ClaudeProject/cognivault && pnpm test -- --run src/plugins/__tests__/metrics.test.ts src/plugins/__tests__/embedding.test.ts</automated>
  </verify>
  <done>
    - MetricsCollection interface has user_id on all per-user metrics
    - contextPacks counter exists with user_id label
    - removeUserMetrics function cleans up all label combinations for a userId
    - Embedding plugin creates per-user EmbeddingProvider from registry users
    - getUserEmbedder(userId) returns correct provider or throws
    - Registry events (added/removed/updated) correctly manage embedder lifecycle
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Decouple qdrant from global embedder, make OPENAI_API_KEY optional, add getUserDbById to db plugin</name>
  <files>
    src/config.ts
    src/plugins/qdrant.ts
    src/plugins/db.ts
    src/plugins/__tests__/db.test.ts
  </files>
  <action>
    **Config (src/config.ts):**
    1. Change `OPENAI_API_KEY: z.string().min(1, ...)` to `OPENAI_API_KEY: z.string().optional()` -- all embedding now uses per-user keys from users.json
    2. No other changes

    **Qdrant plugin (src/plugins/qdrant.ts):**
    1. Add `import { DIMENSION_MAP } from '../lib/embedding.js';` and `import { config } from '../config.js';`
    2. Replace `fastify.embedder.dimensions` with `DIMENSION_MAP[config.EMBEDDING_MODEL]` when creating collection
    3. Add a guard: if DIMENSION_MAP[config.EMBEDDING_MODEL] is undefined, throw an error with known models
    4. Change fp() dependencies from `['embedder']` to `[]` -- qdrant no longer depends on embedding plugin
    5. This is important: qdrant must register BEFORE embedding now (embedding depends on registry, qdrant doesn't)

    **DB plugin (src/plugins/db.ts):**
    1. Add `getUserDbById` to the FastifyInstance module augmentation (alongside existing request decorators)
    2. Add a new fastify.decorate('getUserDbById', ...) that looks up from the existing userDbs Map and returns the db instance, throws if not found
    3. This is a fastify-level accessor (not request-scoped) for pipeline use outside request context
    4. Existing request decorators (getUserDb, getUserQdrant) remain unchanged

    **DB test updates (src/plugins/__tests__/db.test.ts):**
    1. Add test: getUserDbById returns correct DB for existing user
    2. Add test: getUserDbById throws for unknown user
  </action>
  <verify>
    <automated>cd /Users/cytryx/ClaudeProject/cognivault && pnpm test -- --run src/plugins/__tests__/db.test.ts src/plugins/__tests__/qdrant.test.ts && pnpm typecheck</automated>
  </verify>
  <done>
    - OPENAI_API_KEY is optional in config schema
    - Qdrant plugin uses DIMENSION_MAP for collection creation, no longer depends on embedder
    - getUserDbById(userId) fastify-level accessor works from db plugin
    - TypeScript compilation passes with the removed fastify.embedder type
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
After both tasks, run full suite to confirm no regressions:
```bash
pnpm test -- --run && pnpm check
```
All plugins compile. Metrics carry user_id labels. Embedding is per-user. Qdrant decoupled. DB has fastify-level accessor.
</verification>

<success_criteria>
- MetricsCollection interface has user_id on all per-user metrics (OBS-01 foundation)
- removeUserMetrics helper cleans up stale labels on user removal
- contextPacks counter tracks context pack assembly per user
- Embedding plugin manages Map<userId, EmbeddingProvider> with full registry event lifecycle
- getUserEmbedder(userId) works at fastify level (not request level)
- Qdrant plugin creates collection without depending on global embedder
- getUserDbById(userId) provides fastify-level DB access for pipeline
- OPENAI_API_KEY is optional in config
- All tests pass, typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-per-user-indexing-and-routes/18-01-SUMMARY.md`
</output>
