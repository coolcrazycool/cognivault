---
phase: 12-prometheus-metrics-dashboard-in-separate-container
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/metrics.ts
  - src/plugins/pipeline.ts
  - src/plugins/__tests__/metrics.test.ts
  - src/plugins/__tests__/pipeline.test.ts
autonomous: true
requirements:
  - MON-01
  - MON-02

must_haves:
  truths:
    - "Three new pipeline metrics (embedding_requests_total, chunks_processed_total, pipeline_duration_seconds) appear in /metrics output"
    - "Embedding call count increments when pipeline processes a file"
    - "Chunk count increments by number of chunks produced"
    - "Pipeline duration histogram records per-file processing time"
  artifacts:
    - path: "src/plugins/metrics.ts"
      provides: "Extended MetricsCollection with 3 new pipeline metrics"
      contains: "cognivault_embedding_requests_total"
    - path: "src/plugins/pipeline.ts"
      provides: "Instrumentation calls for new metrics"
      contains: "fastify.metrics.embeddingRequests"
  key_links:
    - from: "src/plugins/pipeline.ts"
      to: "src/plugins/metrics.ts"
      via: "fastify.metrics decoration"
      pattern: "fastify\\.metrics\\.(embeddingRequests|chunksProcessed|pipelineDuration)"
---

<objective>
Add three new pipeline metrics to the CogniVault metrics plugin and instrument the pipeline plugin to record them.

Purpose: The Indexing dashboard (Plan 03) needs embedding call counts, chunk throughput, and pipeline duration metrics. These must exist in the `/metrics` output before dashboards can visualize them.
Output: Extended metrics.ts with 3 new metrics, instrumented pipeline.ts, passing tests.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/plugins/metrics.ts
@src/plugins/pipeline.ts
@src/plugins/__tests__/metrics.test.ts
@src/plugins/__tests__/pipeline.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/plugins/metrics.ts:
```typescript
interface MetricsCollection {
  searchDuration: Histogram<'type'>;
  searchRequests: Counter<'type'>;
  indexQueueDepth: Gauge;
  staleVectorCleanups: Counter;
}
```

Pattern: Per-instance Registry (not global default), all metrics pass `registers: [register]`.
Pattern: Metrics decorated on fastify via `fastify.decorate('metrics', { ... })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pipeline metrics to MetricsCollection</name>
  <files>src/plugins/metrics.ts, src/plugins/__tests__/metrics.test.ts</files>
  <action>
In src/plugins/metrics.ts:

1. Add three new fields to the MetricsCollection interface:
   - `embeddingRequests: Counter` — Total embedding API calls
   - `chunksProcessed: Counter` — Total chunks processed through pipeline
   - `pipelineDuration: Histogram` — Per-file pipeline processing duration

2. Inside metricsPlugin(), create the three new prom-client instances (following the existing pattern with `registers: [register]`):
   - `new Counter({ name: 'cognivault_embedding_requests_total', help: 'Total number of embedding API calls made', registers: [register] })`
   - `new Counter({ name: 'cognivault_chunks_processed_total', help: 'Total number of chunks processed through the indexing pipeline', registers: [register] })`
   - `new Histogram({ name: 'cognivault_pipeline_duration_seconds', help: 'End-to-end duration of file processing through the indexing pipeline', buckets: [0.05, 0.1, 0.25, 0.5, 1, 2.5, 5, 10, 30], registers: [register] })`

3. Add the three new metrics to the `fastify.decorate('metrics', { ... })` object.

In src/plugins/__tests__/metrics.test.ts:

4. Add test case(s) verifying the /metrics endpoint output contains the three new metric names: `cognivault_embedding_requests_total`, `cognivault_chunks_processed_total`, `cognivault_pipeline_duration_seconds`. Follow the existing test pattern for metric name assertions.
  </action>
  <verify>
    <automated>pnpm test -- --run src/plugins/__tests__/metrics.test.ts</automated>
  </verify>
  <done>MetricsCollection interface has 3 new fields, /metrics output contains all 3 new metric names, all existing and new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Instrument pipeline with new metrics</name>
  <files>src/plugins/pipeline.ts, src/plugins/__tests__/pipeline.test.ts</files>
  <action>
In src/plugins/pipeline.ts:

1. In the `embedAndUpsert` function, after `await fastify.embedder.embed(...)` returns:
   - `fastify.metrics.embeddingRequests.inc()` — count each embedding API call
   - `fastify.metrics.chunksProcessed.inc(chunks.length)` — count chunks processed

2. In the `processMarkdown` function, after `await fastify.embedder.embed(...)` returns:
   - `fastify.metrics.embeddingRequests.inc()` — count each embedding API call
   - `fastify.metrics.chunksProcessed.inc(chunks.length)` — count chunks processed
   Note: processMarkdown has its own embed call separate from embedAndUpsert — both need instrumentation.

3. In the `processCreatedOrUpdated` function, wrap the entire body in a pipeline duration timer:
   ```typescript
   const end = fastify.metrics.pipelineDuration.startTimer();
   try {
     // existing switch logic
   } finally {
     end();
   }
   ```

In src/plugins/__tests__/pipeline.test.ts:

4. Ensure the mock `fastify.metrics` object includes the 3 new metric properties:
   - `embeddingRequests: { inc: vi.fn() }`
   - `chunksProcessed: { inc: vi.fn() }`
   - `pipelineDuration: { startTimer: vi.fn(() => vi.fn()) }`

5. Add test assertions verifying:
   - `embeddingRequests.inc` is called when processing a file
   - `chunksProcessed.inc` is called with the chunk count
   - `pipelineDuration.startTimer` is called on file processing
  </action>
  <verify>
    <automated>pnpm test -- --run src/plugins/__tests__/pipeline.test.ts</automated>
  </verify>
  <done>Pipeline records embedding calls, chunk counts, and per-file duration in metrics. All pipeline tests pass including new assertions.</done>
</task>

</tasks>

<verification>
pnpm test -- --run src/plugins/__tests__/metrics.test.ts src/plugins/__tests__/pipeline.test.ts
pnpm typecheck
</verification>

<success_criteria>
- /metrics output includes cognivault_embedding_requests_total, cognivault_chunks_processed_total, cognivault_pipeline_duration_seconds
- Pipeline increments embedding and chunk counters on file processing
- Pipeline records duration histogram on file processing
- All tests pass, typecheck clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-prometheus-metrics-dashboard-in-separate-container/12-01-SUMMARY.md`
</output>
