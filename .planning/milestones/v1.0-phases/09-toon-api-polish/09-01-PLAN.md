---
phase: 09-toon-api-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugins/toon.ts
  - src/plugins/__tests__/toon.test.ts
  - src/plugins/error-handler.ts
  - src/plugins/auth.ts
  - src/app.ts
  - package.json
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "POST with Content-Type: text/toon body is decoded and validated against TypeBox schemas"
    - "Response is TOON-encoded when Accept header contains text/toon"
    - "Response is TOON-encoded when Content-Type is text/toon (format symmetry)"
    - "Response is JSON when Accept is application/json or unspecified"
    - "Invalid TOON body returns 400 with code INVALID_TOON"
    - "Error responses (401, 400, 500) are TOON-serialized when client requested TOON"
    - "Health/readiness endpoints always return JSON regardless of Accept header"
  artifacts:
    - path: "src/plugins/toon.ts"
      provides: "TOON content-type parser and onSend response hook"
      exports: ["default"]
    - path: "src/plugins/__tests__/toon.test.ts"
      provides: "Integration tests for TOON content negotiation"
      min_lines: 80
  key_links:
    - from: "src/plugins/toon.ts"
      to: "@toon-format/toon"
      via: "import encode, decode"
      pattern: "import.*from.*@toon-format/toon"
    - from: "src/app.ts"
      to: "src/plugins/toon.ts"
      via: "app.register(toonPlugin)"
      pattern: "register.*toon"
    - from: "src/plugins/error-handler.ts"
      to: "@toon-format/toon"
      via: "encode error payload when TOON requested"
      pattern: "encode.*error|text/toon"
---

<objective>
Add TOON content negotiation to CogniVault so agents can send and receive TOON-formatted bodies for ~40% token savings on tabular data.

Purpose: AI agents consume fewer tokens when search results and context packs use TOON's compact tabular encoding.
Output: TOON plugin (parser + serializer), TOON-aware error handler, TOON-aware auth plugin, integration tests.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-toon-api-polish/09-CONTEXT.md
@.planning/phases/09-toon-api-polish/09-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/app.ts:
```typescript
export async function buildApp(opts?: BuildAppOptions): Promise<FastifyInstance>
// Plugin registration order: error-handler -> auth -> [infrastructure] -> feature routes
```

From src/plugins/error-handler.ts:
```typescript
// Current error handler — uses _request (unused). Must be updated to check request.headers.accept
fastify.setErrorHandler((error: FastifyError, _request: FastifyRequest, reply: FastifyReply) => {
  // returns { error: { code, message } }
});
export default fp(errorHandlerPlugin, { name: 'error-handler' });
```

From src/plugins/auth.ts:
```typescript
// Auth plugin — uses @fastify/bearer-auth with addHook: false, then manual onRequest hook
// skipAuth pattern via request.routeOptions.config
// Auth errors are thrown as Error objects and caught by the error handler (setErrorHandler)
// Therefore 401 TOON-awareness is handled by the error handler, not auth.ts directly
if ((request.routeOptions.config as unknown as Record<string, unknown>)?.skipAuth) { return; }
export default fp(authPlugin, { name: 'auth' });
```

From src/features/health/routes.ts (skipAuth pattern):
```typescript
fastify.get('/health', { config: { skipAuth: true }, schema: healthSchema }, handler);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install @toon-format/toon and create TOON plugin with tests</name>
  <files>package.json, pnpm-lock.yaml, src/plugins/toon.ts, src/plugins/__tests__/toon.test.ts</files>
  <behavior>
    - Test: POST with Content-Type text/toon and valid TOON body is decoded and route handler receives parsed object
    - Test: POST with Content-Type text/toon and invalid TOON body returns 400 with { error: { code: 'INVALID_TOON' } }
    - Test: GET with Accept: text/toon returns Content-Type text/toon and TOON-encoded body
    - Test: GET with Accept: application/json returns JSON (default behavior)
    - Test: GET without Accept header returns JSON (default)
    - Test: GET with unsupported Accept (text/xml) falls back to JSON
    - Test: POST with Content-Type: text/toon (no Accept header) returns TOON response (format symmetry)
    - Test: Health endpoint with Accept: text/toon still returns JSON (skipAuth routes excluded)
    - Test: Content-Type: text/toon; charset=utf-8 is accepted (regex matching)
  </behavior>
  <action>
    1. Run `pnpm add @toon-format/toon`.
    2. Create `src/plugins/__tests__/toon.test.ts` FIRST (TDD RED phase):
       - Build a minimal Fastify app in beforeAll: register error-handler, auth (with a test API key), toon plugin, then a test route (POST /test that echoes body, GET /test that returns { data: 'hello' }, and health route with skipAuth config).
       - Use `app.inject()` for all tests.
       - Import `encode, decode` from `@toon-format/toon` to create test payloads and verify responses.
       - Include the format symmetry test: POST with Content-Type: text/toon but NO Accept header should return a TOON response (Content-Type decides response format).
       - Run `pnpm test -- --run src/plugins/__tests__/toon.test.ts` to confirm tests FAIL (RED).
    3. Create `src/plugins/toon.ts` as a Fastify plugin wrapped with `fp()` (name: 'toon') (TDD GREEN phase):
       - `addContentTypeParser(/^text\/toon/, { parseAs: 'string' }, handler)` -- uses regex to handle charset suffixes. In the handler, call `decode(body)` from `@toon-format/toon`. On decode failure, create an error with statusCode=400 and pass to `done(err, undefined)`. The error handler will catch this and produce the standard error response -- but to get code `INVALID_TOON`, add a custom property (e.g., `err.code = 'INVALID_TOON'`) so the error handler can map it.
       - `addHook('onSend', async (request, reply, payload))` -- check `request.routeOptions.config` for skipAuth (health/readiness = JSON only). Determine if TOON is requested: `const accept = request.headers.accept ?? ''` and `const contentType = request.headers['content-type'] ?? ''`. Check `accept.includes('text/toon') || contentType.includes('text/toon')` (format symmetry per locked decision: Content-Type: text/toon alone triggers TOON response). If TOON requested, `JSON.parse(payload as string)`, call `encode(obj)`, set `reply.header('Content-Type', 'text/toon')`, return encoded string. On any encode failure, do NOT fall back to JSON -- return best-effort TOON output. Per locked decision ("let the format handle it"), if `encode()` throws, re-throw the error rather than silently falling back to JSON.
    4. Run `pnpm test -- --run src/plugins/__tests__/toon.test.ts` to confirm tests PASS (GREEN).
  </action>
  <verify>
    <automated>pnpm test -- --run src/plugins/__tests__/toon.test.ts</automated>
  </verify>
  <done>TOON plugin parses text/toon requests, serializes text/toon responses (triggered by Accept OR Content-Type), falls back to JSON only for unrecognized Accept values, and skips health/readiness routes. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Make error handler TOON-aware, verify auth 401 path, and register plugin in app.ts</name>
  <files>src/plugins/error-handler.ts, src/plugins/auth.ts, src/app.ts, src/plugins/__tests__/toon.test.ts</files>
  <action>
    1. Update `src/plugins/error-handler.ts`:
       - Import `encode` from `@toon-format/toon`.
       - Change `_request` to `request` in the setErrorHandler callback signature.
       - After building the `{ error: { code, message } }` payload, check if TOON is requested:
         - `const accept = request.headers.accept ?? ''`
         - `const contentType = request.headers['content-type'] ?? ''`
         - `const wantToon = accept.includes('text/toon') || contentType.includes('text/toon')`
         - Check if health/readiness route: `const isHealthRoute = (request.routeOptions?.config as Record<string, unknown>)?.skipAuth`
         - If `wantToon && !isHealthRoute`: set `reply.header('Content-Type', 'text/toon')` and `reply.send(encode(payload))`.
         - Otherwise: send as JSON (current behavior).
       - For the INVALID_TOON code: check `error.code === 'INVALID_TOON'` in `mapErrorToCode` and return `'INVALID_TOON'` for it. This handles the custom code set in the toon plugin's parser error.
    2. Verify `src/plugins/auth.ts` 401 path: Auth errors thrown in the onRequest hook are caught by Fastify's `setErrorHandler`. Since the error handler now checks Accept/Content-Type for TOON formatting, 401 auth errors will automatically be TOON-serialized when the client sent Accept: text/toon or Content-Type: text/toon. No changes to auth.ts code are needed -- the error handler handles it. Document this in a code comment in error-handler.ts: `// Auth 401 errors flow through here -- TOON-awareness applies to all error codes including UNAUTHORIZED`.
    3. Update `src/app.ts`:
       - Import toonPlugin: `import toonPlugin from './plugins/toon.js'`
       - Register AFTER auth but BEFORE infrastructure plugins: `await app.register(toonPlugin)` -- placed after `await app.register(authPlugin)` and before `await app.register(vaultPlugin)`.
    4. Add tests to `src/plugins/__tests__/toon.test.ts`:
       - Test: 401 error with Accept: text/toon returns TOON-serialized error with { error: { code: 'UNAUTHORIZED' } }
       - Test: 401 error with Content-Type: text/toon (no Accept) returns TOON-serialized error (format symmetry for errors too)
       - Test: 400 validation error with Accept: text/toon returns TOON-serialized error
       - Test: 401 error without Accept: text/toon returns JSON error (unchanged behavior)
    5. Run full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Error responses are TOON-serialized when client sends Accept: text/toon or Content-Type: text/toon. Auth 401 errors are TOON-serialized through the error handler path (verified by test). app.ts registers toon plugin in correct order. Full test suite passes with no regressions.</done>
</task>

</tasks>

<verification>
- `pnpm test` -- full suite passes
- `pnpm check` -- typecheck + lint + format pass
- TOON request parse: `app.inject({ method: 'POST', url: '/api/vault/search', headers: { authorization: 'Bearer test-key', 'content-type': 'text/toon', accept: 'text/toon' }, payload: encode({ query: 'test' }) })` returns 200 with text/toon content-type
- JSON default: same request without accept/content-type headers returns JSON
- Health exclusion: GET /health with Accept: text/toon still returns application/json
- Format symmetry: POST with Content-Type: text/toon but no Accept header returns TOON response
- Auth error symmetry: request with Content-Type: text/toon and no auth returns TOON-serialized 401
</verification>

<success_criteria>
- All TOON content negotiation tests pass
- Error responses honor Accept/Content-Type for format selection (format symmetry)
- TOON encode failures do not silently fall back to JSON
- Health/readiness endpoints always return JSON
- Auth 401 errors are TOON-serialized when TOON requested
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/09-toon-api-polish/09-01-SUMMARY.md`
</output>
