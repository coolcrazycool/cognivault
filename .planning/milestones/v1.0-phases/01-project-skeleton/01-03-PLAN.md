---
phase: 01-project-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - Dockerfile
  - docker-compose.yml
  - .dockerignore
autonomous: false
requirements: [INF-06]

must_haves:
  truths:
    - "docker compose up builds and starts the cognivault service"
    - "Health endpoint responds via Docker-exposed port"
    - "Qdrant sidecar starts alongside cognivault"
    - "Qdrant healthcheck passes before cognivault starts"
    - "Vault directory is bind-mounted read-only into container"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage Docker build (build + production)"
      contains: "FROM node:22-slim"
    - path: "docker-compose.yml"
      provides: "Service + Qdrant sidecar orchestration"
      contains: "qdrant"
    - path: ".dockerignore"
      provides: "Excludes node_modules, dist, .env from Docker context"
      contains: "node_modules"
  key_links:
    - from: "Dockerfile"
      to: "package.json"
      via: "COPY for dependency install layer"
      pattern: "COPY package.json"
    - from: "docker-compose.yml"
      to: "Dockerfile"
      via: "builds cognivault service from Dockerfile"
      pattern: "build:\\s*\\."
    - from: "docker-compose.yml"
      to: ".env.example"
      via: "references same env vars"
      pattern: "COGNIVAULT_API_KEY"
---

<objective>
Containerize CogniVault with Docker and set up Qdrant sidecar via docker-compose.

Purpose: Enable the full deployment target -- a single `docker compose up` that runs the CogniVault service alongside Qdrant. This is the final deliverable of Phase 1 and the deployment pattern all subsequent phases inherit.
Output: Dockerfile (multi-stage), docker-compose.yml (service + Qdrant), .dockerignore, verified with smoke test.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton/01-CONTEXT.md
@.planning/phases/01-project-skeleton/01-RESEARCH.md
@.planning/phases/01-project-skeleton/01-02-SUMMARY.md
@CLAUDE.md
@.claude/skills/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile, docker-compose, and .dockerignore</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore</files>
  <action>
1. Create .dockerignore: node_modules, dist, .env, .git, .planning, .claude, coverage, *.md (except Dockerfile-related), .DS_Store.

2. Create Dockerfile per RESEARCH.md example with these specifics:
   - Stage 1 "build": FROM node:22-slim AS build. Enable corepack (`corepack enable`). If corepack signature verification fails, set `ENV COREPACK_INTEGRITY_KEYS=""` before enable (see RESEARCH.md Pitfall 2). WORKDIR /app. COPY package.json and pnpm-lock.yaml first (layer caching per RESEARCH.md Pitfall 6). RUN `pnpm install --frozen-lockfile`. COPY tsconfig.json and src/. RUN `pnpm run build`.
   - Stage 2 "production": FROM node:22-slim AS production. Enable corepack. WORKDIR /app. ENV NODE_ENV=production. COPY package.json and pnpm-lock.yaml. RUN `pnpm install --frozen-lockfile --prod`. COPY --from=build /app/dist ./dist. USER node. EXPOSE 3000. CMD ["node", "dist/server.js"].

3. Create docker-compose.yml per RESEARCH.md example:
   - cognivault service: build from ".", ports ${PORT:-3000}:3000, environment vars (COGNIVAULT_API_KEY, VAULT_PATH=/vault, QDRANT_URL=http://qdrant:6333, LOG_LEVEL), volumes bind mount ${VAULT_PATH:-./__vault}:/vault:ro, depends_on qdrant with condition service_healthy.
   - qdrant service: image qdrant/qdrant:v1.13.6 (pin version per user decision), ports 6333:6333 and 6334:6334, volume qdrant_data:/qdrant/storage, healthcheck using wget to /readyz with interval 5s, timeout 3s, retries 5.
   - Named volume: qdrant_data.

4. Create a temporary __vault directory with a dummy file so the bind mount works for testing:
   `mkdir -p __vault && echo "test" > __vault/.gitkeep`
  </action>
  <verify>
    <automated>cd /Users/cytryx/ClaudeProject/cognivault && docker compose build cognivault 2>&1 | tail -5</automated>
  </verify>
  <done>Dockerfile builds successfully in both stages. docker-compose.yml is valid YAML with both services defined. .dockerignore excludes node_modules and dist from build context.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Docker stack end-to-end</name>
  <action>Human verifies the complete Phase 1 deliverable: Fastify service with health endpoints, API key auth, and Docker deployment with Qdrant sidecar all working together.</action>
  <what-built>Complete Phase 1 skeleton: Fastify service with health endpoints, API key auth, Docker deployment with Qdrant sidecar. The full docker compose stack.</what-built>
  <how-to-verify>
    1. Create a .env file: `echo "COGNIVAULT_API_KEY=my-secret-key" > .env && echo "VAULT_PATH=./__vault" >> .env`
    2. Run `docker compose up -d` and wait for both services to start
    3. Test health: `curl http://localhost:3000/health` -- should return 200 with status "ok"
    4. Test readiness: `curl http://localhost:3000/ready` -- should return 200 with status "ready"
    5. Test auth rejection: `curl http://localhost:3000/` -- should return 401
    6. Test auth success: `curl -H "Authorization: Bearer my-secret-key" http://localhost:3000/` -- should return 404 (no routes yet, but not 401)
    7. Check Qdrant: `curl http://localhost:6333/readyz` -- should return 200
    8. Cleanup: `docker compose down`
  </how-to-verify>
  <verify>Human confirms all 8 verification steps pass</verify>
  <done>Human approves the full Docker stack works end-to-end</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `docker compose build` succeeds
2. `docker compose up -d` starts both services
3. `curl http://localhost:3000/health` returns 200
4. `curl http://localhost:6333/readyz` returns 200
5. Auth rejects without key, accepts with key
</verification>

<success_criteria>
- Docker image builds via multi-stage Dockerfile
- docker compose up starts cognivault + Qdrant
- Health/readiness endpoints respond through Docker
- Qdrant sidecar is healthy
- Vault directory is mounted into container
- Human verifies the full stack works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-skeleton/01-03-SUMMARY.md`
</output>
