---
phase: 13-search-reindex-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/search/service.ts
  - src/features/search/__tests__/routes.test.ts
autonomous: true
requirements:
  - RET-05

must_haves:
  truths:
    - "Semantic search with folder filter returns only results whose path starts with the folder prefix"
    - "Hybrid search with folder filter returns only results from the requested folder (semantic leg filtered)"
  artifacts:
    - path: "src/features/search/service.ts"
      provides: "Folder post-filter in semantic() method"
      contains: "folderPrefix"
    - path: "src/features/search/__tests__/routes.test.ts"
      provides: "Tests for semantic and hybrid folder filtering"
      contains: "folder filter in semantic"
  key_links:
    - from: "src/features/search/service.ts semantic()"
      to: "hit.payload.path"
      via: "path.startsWith(folderPrefix) post-filter"
      pattern: "folderPrefix.*startsWith"
    - from: "src/features/search/service.ts hybrid()"
      to: "semantic() with folder filter"
      via: "hybrid calls semantic which now applies folder filter"
      pattern: "this\\.semantic"
---

<objective>
Add folder post-filter to SearchService.semantic() so semantic and hybrid searches respect the folder filter.

Purpose: Currently folder filter only works in lexical search (post-filter via path.startsWith). Semantic search and the semantic leg of hybrid search ignore the folder parameter, leaking results from outside the requested folder.
Output: Fixed semantic() method with folder post-filter, new test cases confirming the behavior.
</objective>

<execution_context>
@/Users/cytryx/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cytryx/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-search-reindex-correctness/13-RESEARCH.md
@src/features/search/service.ts
@src/features/search/__tests__/routes.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/features/search/service.ts:
```typescript
// SearchFilters includes: tags?, project?, status?, type?, folder?
// folder is a string prefix (e.g., "Projects/")

// lexical() already has this pattern (line 67, 99-101):
const folderPrefix = filters.folder;
// ...
.filter(
  (p) => folderPrefix === undefined || (p.payload?.path ?? '').startsWith(folderPrefix),
)

// semantic() is MISSING this filter — results go straight from Qdrant to map()
```

From src/features/search/__tests__/routes.test.ts:
```typescript
// MOCK_SCORED_POINTS has two entries:
//   uuid-1: path 'notes/test.md'
//   uuid-2: path 'notes/other.md'
// These are used for semantic search mock responses
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add folder post-filter to semantic() and add tests</name>
  <files>src/features/search/service.ts, src/features/search/__tests__/routes.test.ts</files>
  <behavior>
    - Test: semantic search with folder filter "notes/" returns only results with paths starting with "notes/"
    - Test: semantic search with folder filter excludes results outside the folder (mock returns mixed paths, only matching paths survive)
    - Test: hybrid search with folder filter returns only results from the requested folder in both semantic and lexical legs
  </behavior>
  <action>
In `src/features/search/service.ts`, modify the `semantic()` method to add a folder post-filter AFTER the text-exists filter and BEFORE the map, mirroring what `lexical()` already does:

1. Extract `const folderPrefix = filters.folder;` at the top of `semantic()`
2. Add `.filter((hit) => folderPrefix === undefined || (hit.payload?.path ?? '').startsWith(folderPrefix))` between the existing `.filter(...)` for null text and the `.map(...)` call
3. Preserve the existing TODO comment about Qdrant text index on path field

In `src/features/search/__tests__/routes.test.ts`, add test cases:

1. "semantic search with folder filter returns only matching paths" — Set up `mockQdrantSearch` to return points with mixed paths (e.g., `'Projects/alpha.md'` and `'notes/other.md'`), call POST /semantic with `filters: { folder: 'Projects/' }`, assert only the Projects/ result is returned.

2. "hybrid search with folder filter excludes results outside folder" — Set up both `mockQdrantSearch` and `mockQdrantScroll` to return points with mixed paths, call POST /hybrid with `filters: { folder: 'Projects/' }`, assert all returned results have paths starting with 'Projects/'.

NOTE: The fix to `semantic()` automatically fixes the hybrid semantic leg since `hybrid()` calls `this.semantic()` internally. No changes needed in `hybrid()`.
  </action>
  <verify>
    <automated>cd /Users/cytryx/ClaudeProject/cognivault && pnpm test -- --run src/features/search/__tests__/routes.test.ts</automated>
  </verify>
  <done>
    - semantic() applies folder post-filter via path.startsWith()
    - New tests pass: semantic with folder filter, hybrid with folder filter
    - All existing search tests still pass
  </done>
</task>

</tasks>

<verification>
- `pnpm test -- --run src/features/search/__tests__/routes.test.ts` passes with new folder filter tests
- `pnpm typecheck` passes
- `pnpm check` passes
</verification>

<success_criteria>
- Semantic search with folder filter returns only results whose path starts with the folder prefix
- Hybrid search with folder filter returns only folder-matching results from both legs
- All existing search tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/13-search-reindex-correctness/13-01-SUMMARY.md`
</output>
